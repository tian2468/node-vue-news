#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_mddir@1.1.1@mddir/src/mddir.js" "$@"
  ret=$?
else 
  node  "$basedir/../_mddir@1.1.1@mddir/src/mddir.js" "$@"
  ret=$?
fi
exit $ret
